name: projeto_planta_realidade_aumentada
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: any
  firebase_auth: any
  firebase_analytics: any
  cupertino_icons: ^1.0.6
  google_generative_ai: ^0.4.3
  cloud_firestore: any
  provider: any
  camera: any
  shared_preferences: any
  google_sign_in: ^6.2.1
  google_sign_in_web: any
  image_picker: ^1.1.2
  flutter_dotenv: ^5.1.0
  http: any
  google_fonts: ^6.2.1
  flutter_markdown: ^0.7.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
    - assets/panda_talking.gif
    - assets/panda-hi.gif
    - assets/panda_beleza.gif